# Python Package Requirements for ICU Sedation Digital Twin Research
#
# This file specifies the exact Python packages and versions needed to
# reproduce the analysis. We pin specific versions because behavior can
# differ subtly between versions, and reproducibility requires that
# anyone trying to verify our results uses the same software environment
# we used.
#
# To install all dependencies in a fresh Python environment, run the
# following command from the directory containing this file:
#
#     pip install -r requirements.txt
#
# We recommend creating a virtual environment first to avoid conflicts
# with other Python projects on your system. On Linux or Mac:
#
#     python -m venv venv
#     source venv/bin/activate
#     pip install -r requirements.txt
#
# On Windows:
#
#     python -m venv venv
#     venv\Scripts\activate
#     pip install -r requirements.txt
#
# Note that exact version numbers are specified using the >= operator,
# meaning these are the minimum versions known to work. We have tested
# the analysis with these versions and slightly newer ones. Much newer
# major versions may introduce breaking changes that would require
# updates to the analysis code.

# Core scientific computing libraries
numpy>=1.24.0,<2.0
pandas>=2.0.0,<3.0
scipy>=1.10.0,<2.0

# Machine learning libraries
scikit-learn>=1.3.0,<2.0
xgboost>=2.0.0,<3.0

# Deep learning framework for LSTM models
tensorflow>=2.13.0,<3.0

# Visualization and plotting
matplotlib>=3.7.0,<4.0
seaborn>=0.12.0,<1.0

# Statistical analysis tools that we may use for confidence intervals
# and significance testing in later analysis phases
statsmodels>=0.14.0,<1.0

# Explainability tools that we plan to add for the final paper
shap>=0.42.0,<1.0
lime>=0.2.0

# BigQuery client for executing the data extraction queries
google-cloud-bigquery>=3.11.0,<4.0
google-auth>=2.22.0,<3.0
db-dtypes>=1.1.0,<2.0
pandas-gbq>=0.19.0,<1.0

# PDF generation for progress reports and documentation
reportlab>=4.0.0,<5.0

# Jupyter for any exploratory notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
